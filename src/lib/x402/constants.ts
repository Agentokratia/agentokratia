// x402 Session Constants

/** Session duration in seconds (1 hour) */
export const SESSION_DURATION_SECONDS = 3600;

/** Refund window in seconds after session expires (24 hours) */
export const REFUND_WINDOW_SECONDS = 86400;
